<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.romantic.dao.CommentMapper" >

    <sql id="Base_Column_List">
      comment_id, user_id, goods_id, user_name, user_name, grade, comment, pic_address, Like_number, Prefix_match, create_time, update_time, is_alive
    </sql>

    <insert id="insertGoodsComment" parameterType="com.romantic.pojo.Comment" >
      INSERT INTO tb_comment (
      <!--<include refid="Base_Column_List" />-->
        comment_id, user_id, goods_id, user_name, user_name, grade, comment, pic_address, Like_number, Prefix_match, create_time, update_time, is_alive
      ) VALUES (
      #{commentId},#{userId},#{goodsId},#{userName},#{grade},#{comment},
      #{picAddress},#{likeNumber},#{prefixMatch},#{createTime}
      )
    </insert>


</mapper>